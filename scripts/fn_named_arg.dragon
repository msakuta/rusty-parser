var x: i32;
x = 123;

fn f(y: i32) {
    x * y;
}

print(f(5 + 5));

fn g(x: f64) -> f64 {
    x * 1.5;
}

print(g(x: 10.3));

fn h(x: i64) -> i64 {
    x * 10;
}

print(h(x : 103));

fn max(a: f64, b: f64) -> f64 {
    if a < b {
        b;
    } else {
        a;
    }
}

print(max(a: 3., b: 5.));
print(max(b: 3., a: 5.));
