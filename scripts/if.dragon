
fn min(x, y) {
    if x < y {
        x
    } else {
        y
    };
}

fn max(x, y) {
    if x > y {
        x
    } else {
        y
    };
}

min(1, 2);
min(2, 1);
min(42, -42);
max(1, 2);
max(2, 1);
max(42, -42);
